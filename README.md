IoC = Inversion of Control

Types:
1. BeanFactory (Interface)
2. ApplicationContext (Interface)

Uses:
1. Create Object from config specification (beans.xml) class implecitly
2. Manage objects or spring beans
3. objects define their dependencies
